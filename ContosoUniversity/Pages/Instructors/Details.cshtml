@page "{id:int}"
@model Instructors.Details

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Instructor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Data.LastName)
        </dt>
        <dd>
            @Model.Data.LastName
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Data.FirstMidName)
        </dt>
        <dd>
            @Model.Data.FirstMidName
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Data.HireDate)
        </dt>
        <dd>
            @Model.Data.HireDate
        </dd>
        <dt>
            Office
        </dt>
        <dd>
            @Model.Data.OfficeAssignmentLocation
        </dd>
    </dl>
</div>
<div>
    <a asp-page="CreateEdit" asp-page-handler="Edit" asp-route-id="@Model.Data.Id">Edit</a> |
    <a asp-page="Index">Back to List</a>
</div>
